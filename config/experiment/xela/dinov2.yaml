# @package _global_
defaults:
  - override /paths: default
  - override /tensorboard: tensorboard_config
  - override /data: xela
  - override /algorithm: xela_dinov2
  - _self_

model_size: tiny
ckpt_path: ~ #/workspace-SR004.nfs2/konovalov/tactile/experiments/pretrain_dinov2_encoder_dinov2/2026.07.08_19-25/checkpoints

task_name: pretrain_dinov2_encoder # pretrain_nopos_linear_encoder
ssl_name: dinov2
ssl_model_size: tiny
experiment_name: ${task_name}_${ssl_name}
seed: 42

hydra:
  job:
    id: ${now:%Y.%m.%d}_${now:%H-%M}
  run:
    dir: ${paths.log_dir}/${experiment_name}/${hydra.job.id}

run_name: Orig_DiNO_with_probe

data:
  features:
    use_spatial_coords: true

trainer:
  max_epochs: 500
  validation_frequency: 5
  save_checkpoint_dir: ${paths.output_dir}/checkpoints

algorithm:
  centering: centering
  encoder:
    _target_: tactile_ssl.model.xela_${model_size}
