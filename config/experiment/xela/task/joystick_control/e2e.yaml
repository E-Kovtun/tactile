# @package _global_
defaults:
  - override /paths: default
  - override /tensorboard: tensorboard_config
  - override /data: xela_joystick_control
  - override /task: xela_joystick_control
  - _self_

ssl_name: e2e
sensor: xela

ckpt_path: ~
resume_id: ~
train_data_budget: 1.0
val_data_budget: 1.0

trainer:
  save_probe_weights_only: false

task:
  checkpoint_encoder: ~
  train_encoder: true
