# @package _global_
defaults:
  - override /paths: default
  - override /tensorboard: tensorboard_config
  - override /data: xela_object
  - override /task: xela_object_classification
  - _self_

sensor: xela
ssl_name: mae
ckpt_path: ~
resume_id: ~

experiment_name: downstream_object_classication_mae
run_name: regular_mae

task:
  checkpoint_encoder: /workspace-SR004.nfs2/konovalov/tactile/experiments/pretrain_transformer_encoder_mae/2026.07.12_16-22/checkpoints/epoch-0500.ckpt
  model_encoder:
    in_chans: 3
    sequence_length: ${int_multiply:${data.window_time},${data.interpolating_freq}}
    time_chunk_size: ${task.model_encoder.sequence_length}
    num_register_tokens: 1
    with_masktoken: True
