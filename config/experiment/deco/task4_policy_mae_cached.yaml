# @package _global_
defaults:
  - /experiment/deco/task4_final_policy_cached
  - _self_

ssl_name: mae
experiment_name: ${task_name}_${ssl_name}
run_name: cached_resnet18_gap_visible_random75_seed${seed}

task:
  model:
    tactile_encoder:
      num_register_tokens: 1
      with_masktoken: false
    checkpoint_encoder: ???
    encoder_checkpoint_key: encoder
