# @package _global_
defaults:
  - /experiment/sock/pose_jepa_official
  - _self_

ssl_name: dinov2
experiment_name: sock_pose_dinov2_official

task:
  encoder_type: dino
  model_encoder:
    num_register_tokens: 1
    with_masktoken: true
