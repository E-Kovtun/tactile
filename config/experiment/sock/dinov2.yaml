# @package _global_
defaults:
  - override /paths: sock
  - override /tensorboard: tensorboard_config
  - override /data: sock
  - override /algorithm: sock_dinov2
  - _self_

model_size: tiny
ckpt_path: null
task_name: pretrain_sock_encoder
ssl_name: dinov2
experiment_name: ${task_name}_${ssl_name}
seed: 42
run_name: sock_dinov2_500ep

trainer:
  max_epochs: 500
  validation_frequency: 5
  save_checkpoint_dir: ${paths.output_dir}/checkpoints
