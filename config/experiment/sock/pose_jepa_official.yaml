# @package _global_
defaults:
  - override /paths: sock
  - override /data: sock_pose
  - override /task: sock_pose_jepa_official
  - _self_

data:
  dataset:
    mode: jepa_official

run_name: centered_60frame_seed${seed}
