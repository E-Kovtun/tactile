# @package _global_
defaults:
  - override /paths: sock
  - override /data: sock_pose
  - override /task: sock_pose_cnn
  - _self_

data:
  dataset:
    mode: official_cnn
  train_dataloader:
    batch_size: 128
  val_dataloader:
    batch_size: 128
  test_dataloader:
    batch_size: 128

run_name: centered_frame30
