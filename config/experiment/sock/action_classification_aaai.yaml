# @package _global_
defaults:
  - /experiment/sock/action_classification
  - _self_

# Released SensTextile protocol: ordered stride-2 windows, chronological tail
# for validation/test, and a fixed-size train sample drawn with replacement.
run_name: temporal_9x5_official_window

data:
  dataset:
    split_protocol: aaai_window
    aaai_val_per_class: 500
    aaai_test_per_class: 1000
    aaai_train_per_class: 4000
